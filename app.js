// Modules, Evey file is module in JS (by default)
// Modules - encapsulated code (only share what we want )
const ahsan = "Ahsan";

const sayHi = (name) => {
  console.log(`Hello there ${name}`);
};

sayHi(ahsan);
